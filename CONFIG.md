# 🔧 NotebookLLM DeepSeek配置

## 🔐 API配置

### 当前LLM提供商
**DeepSeek AI** ✅

### API密钥设置
1. 访问: https://platform.deepseek.com/api_keys
2. 创建新的API密钥
3. 编辑 `backend/main.py` 第17行:
```python
DEEPSEEK_API_KEY = "your_key_here"
```

### API端点
```
https://api.deepseek.com/chat/completions
```

## 📦 支持的模型

### DeepSeek官方模型 ✅
- **deepseek-chat** (推荐，当前使用)
  - 通用聊天模型
  - 快速响应
  - 适合日常对话

- **deepseek-coder** (可选)
  - 代码生成专用
  - 适合技术讨论

### 切换模型
编辑 `backend/main.py` 第18行:
```python
DEEPSEEK_MODEL = "deepseek-chat"  # 改为其他模型
```

## 🔄 迁移指南

### 从OpenAI迁移到DeepSeek
已自动完成！变更包括：
- ✅ API端点更新
- ✅ 认证方式更新
- ✅ 模型配置更新
- ✅ 前端API调用适配

### 返回OpenAI (如需要)
编辑 `backend/main.py`:
```python
DEEPSEEK_API_KEY = "sk-xxx"  # OpenAI密钥
DEEPSEEK_API_URL = "https://api.openai.com/v1/chat/completions"
DEEPSEEK_MODEL = "gpt-3.5-turbo"
```

## 讨论深度设置

1. **shallow** (浅层)
   - 简洁而表面的讨论
   - 只涉及主要观点
   - 适合快速概览

2. **medium** (中等) [推荐]
   - 平衡的、中等深度
   - 涵盖主要和次要观点
   - 最通用的选择

3. **deep** (深层)
   - 深入的、详细的
   - 探索性讨论
   - 适合学术讨论

## 对话长度设置

1. **short** (短篇)
   - 5-10分钟
   - ~800-1200字
   - 快速讨论

2. **medium** (中等) [推荐]
   - 10-20分钟
   - ~1500-2500字
   - 平衡讨论

3. **long** (长篇)
   - 20-30分钟
   - ~3000-4500字
   - 深入讨论

## 语气风格设置

1. **professional** (专业严肃) [推荐]
   - 学术性、商务性
   - 正式、权威

2. **casual** (轻松随意)
   - 友好、日常
   - 非正式、亲近

3. **educational** (教育启蒙)
   - 易于理解
   - 适合学习者

4. **entertaining** (娱乐幽默)
   - 有趣、引人入胜
   - 生动活泼

## 讲者名称

默认值：
- 讲者1: 讲者1
- 讲者2: 讲者2

推荐名称组合：
- 教学场景: 老师 + 学生
- 商务讨论: 主管 + 员工
- 新闻评论: 主持人 + 评论员
- 辩论场景: 正方 + 反方

## 服务器配置

### 后端服务器
- 地址: http://0.0.0.0:8000
- 框架: FastAPI
- ASGI服务器: Uvicorn
- 自动重载: 开启（开发模式）

### 前端服务器
- 地址: http://localhost:3000
- 类型: 静态文件服务器
- 协议: HTTP

## CORS配置

后端已配置CORS，允许：
- 所有来源 (*)
- 所有方法 (GET, POST, PUT, DELETE, etc.)
- 所有头 (Content-Type, Authorization, etc.)
- 凭证: True

## 超时配置

- HTTP请求超时: 60秒
- 连接超时: 5秒

## 日志配置

### 后端日志级别
- INFO: 记录请求和响应
- WARNING: 警告信息
- ERROR: 错误信息

### 前端日志
- 浏览器控制台 (F12)
- Network标签查看API调用

## 环境变量

在 `backend` 目录创建 `.env` 文件：

```env
# API配置
OPENAI_API_KEY=sk-5610a05204284964a0953677a117a9dd
ANTHROPIC_API_KEY=

# 服务器配置
HOST=0.0.0.0
PORT=8000

# 模型配置
MODEL=gpt-3.5-turbo
TEMPERATURE=0.7
MAX_TOKENS=4000
TOP_P=0.9

# 日志配置
LOG_LEVEL=INFO
DEBUG=False
```

## 性能优化建议

1. **更快的响应**
   - 使用gpt-3.5-turbo
   - 设置短篇讨论
   - 减少讨论深度

2. **更高的质量**
   - 使用gpt-4或gpt-4-turbo
   - 设置长篇讨论
   - 增加讨论深度

3. **平衡方案** (推荐)
   - 使用gpt-3.5-turbo
   - 设置中等讨论
   - 使用中等长度

## 错误处理

### 常见错误代码

| 错误 | 原因 | 解决 |
|------|------|------|
| 401 | API密钥无效 | 检查API密钥配置 |
| 429 | 请求过于频繁 | 等待后重试 |
| 500 | 服务器错误 | 查看后端日志 |
| CORS错误 | 跨域问题 | 检查后端CORS配置 |

## 扩展配置

### 自定义LLM服务商

在 `backend/main.py` 中修改：

```python
# 添加新的LLM服务商
async def call_gemini_api(prompt: str, model: str) -> str:
    # Google Gemini API实现
    pass

# 在call_llm_api中添加路由
if provider == "gemini":
    return await call_gemini_api(prompt, model)
```

### 自定义样式

编辑 `frontend/index.html` 的 `<style>` 部分：

```css
/* 修改主颜色 */
body { background: linear-gradient(...); }

/* 修改字体 */
font-family: '你的字体', sans-serif;

/* 修改按钮样式 */
.btn { background: #your-color; }
```

## 数据库集成 (未来功能)

计划添加：
- SQLite/PostgreSQL存储
- 讨论历史记录
- 用户账户管理
- 数据持久化

配置示例：
```python
DATABASE_URL = "sqlite:///./notebookllm.db"
# 或
DATABASE_URL = "postgresql://user:password@localhost/notebookllm"
```

---

**最后更新**: 2024年12月30日
**版本**: 1.0.0
