# 部署前检查清单 - sunnyding.cn

部署前请确认以下所有项目：

## ☑️ 服务器准备

- [ ] 服务器有公网 IP 地址
- [ ] 已安装 Linux 系统 (推荐 Ubuntu 20.04/22.04)
- [ ] 服务器至少 2GB RAM, 1核 CPU, 10GB 存储
- [ ] 可以通过 SSH 登录服务器
- [ ] 服务器可以访问外网

## ☑️ 域名配置

- [ ] 已购买域名 sunnyding.cn
- [ ] DNS A 记录已添加：sunnyding.cn → 服务器IP
- [ ] DNS A 记录已添加：www.sunnyding.cn → 服务器IP
- [ ] DNS 已生效（使用 `nslookup sunnyding.cn` 验证）

## ☑️ 防火墙和端口

- [ ] 80 端口已开放（HTTP）
- [ ] 443 端口已开放（HTTPS）
- [ ] 22 端口已开放（SSH - 可选但推荐）
- [ ] 服务器防火墙规则已配置

### 检查命令
```bash
# 检查端口是否开放
sudo netstat -tlnp | grep -E ':(80|443|22)'

# 配置防火墙（如果需要）
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 22/tcp
sudo ufw enable
```

## ☑️ API 密钥

- [ ] 已注册 DeepSeek 账号
- [ ] 已获取 API 密钥
- [ ] API 密钥已保存在安全的地方
- [ ] 账户有足够的余额/配额

获取地址：https://platform.deepseek.com/

## ☑️ 项目文件

- [ ] 已准备好完整的项目文件
- [ ] 前端文件完整（index.html, config.js, axon-icon.svg）
- [ ] 后端文件完整（main.py, requirements.txt）
- [ ] 部署脚本可用（deploy.sh 或 deploy.ps1）
- [ ] Nginx 配置文件可用（nginx.conf）

## ☑️ 开发环境测试

- [ ] 在本地成功运行前端
- [ ] 在本地成功运行后端
- [ ] 所有功能在本地测试正常
- [ ] API 调用正常工作

### 测试命令
```bash
# 测试后端
cd backend
python main.py
# 访问 http://localhost:8000/health

# 测试前端
cd frontend
python -m http.server 3000
# 访问 http://localhost:3000
```

## ☑️ 安全配置

- [ ] SSH 密钥已配置（推荐）
- [ ] 服务器已更新到最新版本
- [ ] 已禁用 root 密码登录（推荐）
- [ ] 已配置服务器备份策略

## ☑️ 部署工具

选择一种部署方式：

### 自动部署（推荐）
- [ ] 已准备 deploy.sh 脚本
- [ ] 已给予执行权限（`chmod +x deploy.sh`）

### 手动部署
- [ ] 熟悉 Linux 命令行
- [ ] 已阅读完整部署文档（DOMAIN_DEPLOYMENT.md）
- [ ] 理解 Nginx 配置
- [ ] 理解 Systemd 服务配置

## ☑️ 依赖软件版本

确认以下软件版本：

```bash
# Python 版本
python3 --version  # 需要 >= 3.8

# Nginx 版本
nginx -v  # 任意最新版本

# Git 版本（可选）
git --version
```

## ☑️ 部署后验证

部署完成后需要验证：

- [ ] https://sunnyding.cn 可以访问
- [ ] SSL 证书显示为有效
- [ ] 文件上传功能正常
- [ ] AI 对话功能正常
- [ ] 音频生成功能正常
- [ ] 数据分析功能正常
- [ ] 思维导图功能正常
- [ ] 后端 API 响应正常（/api/health）

### 验证命令
```bash
# 健康检查
curl https://sunnyding.cn/api/health

# 服务状态
sudo systemctl status nginx
sudo systemctl status axon-backend

# 日志查看
sudo tail -f /var/log/nginx/error.log
sudo journalctl -u axon-backend -f
```

## ☑️ 监控和维护

- [ ] 已设置日志轮转
- [ ] 已配置监控告警（可选）
- [ ] 已记录服务器登录信息
- [ ] 已保存备份恢复流程

## 📝 重要信息记录

请填写以下信息并妥善保管：

```
服务器信息：
- 服务器 IP: ___________________
- SSH 端口: ___________________
- SSH 用户名: ___________________

域名信息：
- 域名: sunnyding.cn
- DNS 提供商: ___________________
- DNS 管理地址: ___________________

API 信息：
- DeepSeek API Key: ___________________
- API 配额/余额: ___________________

SSL 证书：
- 证书提供商: Let's Encrypt
- 证书路径: /etc/letsencrypt/live/sunnyding.cn/
- 过期时间: ___________________ (自动续期)

备份信息：
- 备份位置: ___________________
- 备份频率: ___________________
```

## 🚀 开始部署

当以上所有项目都已确认，您可以开始部署：

```bash
# 1. 上传项目到服务器
scp -r ./mynotebookllmwebsite user@server-ip:/tmp/

# 2. 登录服务器
ssh user@server-ip

# 3. 移动项目
sudo mv /tmp/mynotebookllmwebsite /var/www/axon

# 4. 运行部署脚本
cd /var/www/axon
chmod +x deploy.sh
sudo ./deploy.sh

# 5. 配置 API 密钥
sudo nano /var/www/axon/backend/.env
# 填入: DEEPSEEK_API_KEY=your_key

# 6. 重启服务
sudo systemctl restart axon-backend

# 7. 访问网站
# 浏览器打开: https://sunnyding.cn
```

## ✅ 部署完成

恭喜！如果一切顺利，您的 Axon AI Research Assistant 现在应该已经在 https://sunnyding.cn 上运行了！

## 📚 相关文档

- [快速部署指南](QUICK_DEPLOY.md)
- [完整部署文档](DOMAIN_DEPLOYMENT.md)
- [Nginx 配置](nginx.conf)
- [使用指南](USAGE.md)

---

**记住：**
- 定期更新系统和依赖包
- 监控 API 使用量和服务器资源
- 定期备份重要数据
- 保管好 API 密钥和服务器密码
